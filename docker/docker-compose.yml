version: "3.2"
services:
  agent:
    image: agent:latest
    container_name: agent
    volumes:
      - ../conf/:/mongomon/conf
    restart: always
    stdin_open: true
    tty: true
    networks:
      - solar
networks:
  solar:
    external:
      name: solar_default
